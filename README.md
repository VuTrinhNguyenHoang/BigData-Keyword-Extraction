# BigData-Keyword-Extraction
A scalable Big Data project to extract keywords (TF-IDF, KeyBERT) and detect trends (BERTopic) from millions of scientific abstracts. Implements a full Medallion Lakehouse (Bronze/Silver/Gold) on Docker using PySpark, Delta Lake, MinIO, OpenSearch, FastAPI, and React.
